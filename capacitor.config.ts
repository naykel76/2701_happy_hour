import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '2701_happy_hour',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
